{
  "name": "mactech-governance-platform",
  "version": "1.0.0",
  "description": "MacTech Enterprise Federal Governance & Risk Management Platform",
  "private": true,
  "scripts": {
    "prepare": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && node scripts/copy-schema.js && npm run build:frontend && node scripts/copy-frontend.js",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "db:migrate": "cd backend && npm run db:migrate",
    "db:seed": "cd backend && npm run db:seed",
    "db:seed:prod": "cd backend && npm run db:seed:prod",
    "start": "cd backend && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
